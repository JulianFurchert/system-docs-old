import * as React from "react"
import * as Ui from "../../ui"

export function CheckboxButton() {
    return <Ui.CheckboxButton>Example</Ui.CheckboxButton>
}

CheckboxButton.defaultProps = {
    width: 200,
    height: 200,
}
